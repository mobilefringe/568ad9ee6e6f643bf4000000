<style>
    #about_bc img{
        width: 100%;
        height: auto;
    }
     #about_bc .row {
         margin: 0;
     }
</style>
<section id="page_banner">
    <div class="main_container hero_container">
        <div>
            <h2 class="page_title text-center"> Better than Google Adwords and Facebook Ads</h2>
            <!--<p class="headline_desc">Working with Retail Maverick will make your life so much easier!</p>-->
        </div>
    </div>
</section>
<section id="about_bc">
    <div class="">
        <div class="main_content" style="padding: initial;">
            <div class="row">
                <div id="mm_page_content" class="details_desc"></div>
                <div id="desktop_container">
                    <script id="desktop_template" type="x-tmpl/mustache">
                       <a href="{{click_link}}" style="{{style}}"> <img src="{{image_url}}"> </a>
                    </script>
                </div>
            </div>
        </div>
    </div>
</section>


<script>
    $(document).ready(function(e){
        init(e);
        loadMallDataCached(renderPage);
    })
    
    function renderPage(){
        var pathArray = window.location.pathname.split( '/' );
        var slug = pathArray[pathArray.length-1];
        prefix = get_prefix();
        console.log(prefix)''
        var pages_json = prefix+"/pages/" + slug + ".json"
        
        if (slug.indexOf("retailmaverick-business-case--2") > -1) {
            var images = getFashionBySlug("retailmaverick-business-case").fashion_images;
            $.each( images , function( key, val ) {
                val.image_url="//mallmaverick.com" + val.photo_url;
                if(val.image_url.indexOf("SLIDE7") > -1)
                {
                   // val.show_click
                    val.click_link = "/registration";
                    
                }
                else {
                     val.click_link ="#";
                     val.style = "cursor: default;"
                }
            });
            
            renderGeneral('#desktop_container','#desktop_template', images);
        }
        else {
            $.getJSON(pages_json).done(function(data) {
                $('.page_title').html(data.title);
                $('#mm_page_content').html(data.body);
            }).fail(function(jqXHR) {
                if (jqXHR.status == 404) {
                    $("#404_msg").fadeIn("fast");
                }
            });
        }
        
        // show_content();
    }
</script>